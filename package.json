{
  "name": "cookie-auth",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "devServer": "cd server && nodemon server.js",
    "prodServer": "cd server && node server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run devServer\" \"npm run client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "js-cookie": "^2.2.1"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
